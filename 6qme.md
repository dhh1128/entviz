# SECURITY (from a Claude Security scan of entviz-js, 2026-07-31, rev 557bcc8, medium effort, 116 files, verified status): ten findings survived a 3-voter panel; two clusters matter and one is a SPEC question that affects every port. CLUSTER A — CONFORMANCE GAP, entviz-js-specific, HIGH: packages/react/docs/comparison-design.md 6.2 requires the SVG compare engine to (1) reject transform on text and extra per-cell instances, (2) recover core only at <=512 bits, (3) RECOMPUTE fingerprint fields rather than trust declared data-* and re-render for self-consistency, (4) reject any inconsistency and never reach IDENTICAL. The shipped validateClosedProfile (packages/core/src/compare.ts:151-168) is a flat tag whitelist satisfying NONE of the four; compareSvg:274 then returns identical. Result: append an opaque rect plus your own glyphs to a genuine render(X), keep every tag inside the whitelist, and you get a green '= Identical' chip while a human viewer sees Y — no preimage, no hash work. This is exactly redteam S3, which reviews/comparison-redteam/rebuttal-01-orchestrator.md adjudicated 'Solid, worth acting on... Keep.' It was specified, ruled on, documented, and then not built — the incident behind cc ledger mode 22. CLUSTER B — SPEC QUESTION, cross-port, MEDIUM: should a presentation prefix be IDENTITY-BEARING? entviz-js parseBech32 (entviz.ts:1273) puts only data.slice(0,-6) in core, dropping BOTH the HRP and the checksum that binds it, with prefixSemantic false. Consequence: nostr npub1<payload> and nsec1<same payload> produce byte-identical cells, nucleus colors, surround bits, ellipse, color bar, blank map and comparisonText, and compareValues/compareComparisonText/compareSvg all return identical — only the small gray label differs. Same for any cosmos-family HRP swap; the 6-char checksum is trivially recomputable. A user confirming 'this is my PUBLIC key, safe to publish' can be walked into publishing the secret one. spec.md:127 already mandates verifying the polymod over hrp_expand(hrp)||data, so the HRP is VALIDATED but not IDENTIFYING — the spec is silent on whether it must bind. Decide it HERE, not in the JS port, because entviz-go/java/rs inherit the same ambiguity, and the fix (prefixSemantic true, folding '<hrp>1'||core into the fingerprint, as did/urn/gitoid/swhid already get; or keep the checksum inside core as parseBitcoin's segwit branch does) changes RENDERED OUTPUT and is therefore a spec change with a conformance-harness impact. Related JS-side siblings: identityKey (compare.ts:46) drops every non-semantic prefix so multihash 1220 vs 1b20 also compare identical; compareComparisonText affirms from cell text alone; and buildCheckPlan (compare-walk.ts:296) sets includeGestalt = d.truncated, so the 'Complete' walk over a <=512-bit value contains ONLY text steps and can return 'no difference found' while contradicting a correct machine verdict. ARTIFACT AT RISK: the full report is at entviz-js/CLAUDE-SECURITY-20260731-170021/ (RESULTS.md, RESULTS.jsonl, revision stamp) but the directory self-gitignores — delete its .gitignore to keep it, or it is gone on the next clean checkout. Nothing was executed: every finding is derived from reading, none carries a demonstrated exploit.
kind: todo
created: 2026-07-31T22:36Z

